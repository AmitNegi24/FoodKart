spring:
  application:
    name: menu-service

  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:foodkart_menu}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

restaurant-service:
  url: http://localhost:8081

server:
  port: 8082

management:
  endpoints:
    web:
      exposure:
        include: health,info