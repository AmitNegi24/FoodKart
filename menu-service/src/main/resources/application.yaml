spring:
  application:
    name: menu-service

  datasource:
    url: jdbc:postgresql://${DB_HOST}:${DB_PORT:5432}/${DB_NAME}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver



  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

#//secrets are exported to .env
jwt:
  secret: ${JWT_SECRET}

restaurant-service:
  url: http://localhost:8081

server:
  port: 8082

management:
  endpoints:
    web:
      exposure:
        include: health,info